#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../semi/bin/semi" "$@"
  ret=$?
else 
  node  "$basedir/../semi/bin/semi" "$@"
  ret=$?
fi
exit $ret
